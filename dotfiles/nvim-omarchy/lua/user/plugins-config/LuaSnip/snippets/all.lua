local ls = require("luasnip")

local sf = require("user.plugins-config.LuaSnip.snippets.latex.snip_funcs")

ls.add_snippets("all", {
	-- sf.pair("(", ")"),
	-- sf.pair("[", "]"),
	-- sf.pair("{", "}"),
	-- sf.pair('"', '"'),
	-- sf.pair("'", "'"),
})
