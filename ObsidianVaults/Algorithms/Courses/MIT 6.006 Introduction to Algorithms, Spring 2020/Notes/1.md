
# What's the problem of algorithms?


[[477c78e0af2df61fa205bcc6cb613ceb_MIT6_006S20_lec1.pdf|pdf notes]]

# What's an algorithm?

# Correctness of algorithm

Mathematical proof

Induction:
1. base
2. step

# Efficiency of algorithm

# Asymptotic notation

- $O(\cdot$) upper bound
- $\Omega(\cdot)$ lower bounds
- $\Theta(\cdot)$ both upper and lower bounds

# Model of computation

What operations on the machine can be performed in $O(1)$ time.

# Word-RAM

Memory is a collection of bytes.

CPU has RAM of 32/64-bits, and can store in RAM an address to operate on.
- 32-bit: Max memory = $2^{32}$ bytes = 4 GB
- 64-bit: Max memory =  $2^{64}$ bytes = 20 exabytes (EB)

# Data structures

